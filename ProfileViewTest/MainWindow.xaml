<Window x:Class="ProfileViewTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProfileViewTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="600">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Margin="10 0 0 10" Content="Choose a Configuration" FontSize="20" />

        <ScrollViewer Margin="10 0 0 0" Grid.Row="1"
                      ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.VerticalScrollBarVisibility="Visible"
                      PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">

            <ListView x:Name="fileView" />

        </ScrollViewer>
        <!--
        <ListView Margin="0 75 0 0"  x:Name="fileView" />
        -->

        <Button Grid.Row="2" Margin="400 10 25 10" Content="Select"/>

    </Grid>

</Window>
